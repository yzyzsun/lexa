def make_adder(a: int): <> (int) -> int {
    val res = fun <> (b: int): int {
        a + b
    };
    res
}

def main(): int {
    val adder5 = make_adder(5);
    val adder10 = make_adder(10);
    val _ = ~printInt(adder5(7));
    val _ = ~printInt(adder10(7));
    0
}