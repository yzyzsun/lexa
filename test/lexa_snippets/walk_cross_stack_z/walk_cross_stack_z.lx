effectz E {
    dcl op: () -> int
}

def main [;] (): int {
    handle <> {
        handle <e1> {
            ~printInt(raise e1.op());
            0
        } with e2: E {
            hdl_1 op(k) {
                ~printInt(1);
                resume_final k 2;
                0
            }
        }
    } with e1: E {
        def op() {
            ~printInt(3);
            0
        }
    }
}