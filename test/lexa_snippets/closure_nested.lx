def f(x: int): int {
    val z = 1;
    val g = fun <> (x: int): int {
        val y = 2;
        val h = fun <> (x: int): int {
            x + y + z
        };
        h(x)
    };
    g(x)
}

def main(): int {
    val _ = ~printInt(f(3));
    0
}