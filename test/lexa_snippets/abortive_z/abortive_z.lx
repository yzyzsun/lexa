effectz E {
    op: () -> int
}

def main [;] (): int {
    handle <> {
        raise e.op();
        ~printInt(1)
    } with e: E {
        exc op() {
            ~printInt(2);
            0
        }
    };
    ~printInt(3);
    0
}